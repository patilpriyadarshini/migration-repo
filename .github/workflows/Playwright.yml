name: Playwright Tests
on:
  push:
    branches: [ main ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'
        cache-dependency-path: ./Phase2_frontend
    - name: Install dependencies    
      working-directory: ./Phase2_frontend
      run: npm ci
    - name: Install Playwright Browsers
      working-directory: ./Phase2_frontend
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      working-directory: ./Phase2_frontend
      run: npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: Phase2_frontend/playwright-report/
        retention-days: 30
    - name: Send report to n8n
      run: |
        curl -X POST \
        -H "Content-Type: application/json" \
        -d "$(cat playwright-report/index.html | jq -Rs .)" \
        http://34.71.202.137:5678/webhook/playwright
